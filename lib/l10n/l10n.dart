import 'package:flutter/material.dart';

class L10n {
  static const all = [
    azLocale,
    ruLocale,
  ];

  static const azLocale = Locale('az', 'AZ');
  static const ruLocale = Locale('ru', 'RU');
}
